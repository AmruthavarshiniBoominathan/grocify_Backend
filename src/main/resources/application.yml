server:
  port: 8080

spring:
  datasource:
    url: jdbc:mysql://localhost:3306/grocify?createDatabaseIfNotExist=true
    username: root
    password: Amrutha@10
    driver-class-name: com.mysql.cj.jdbc.Driver
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        dialect: org.hibernate.dialect.MySQLDialect
    database: mysql
